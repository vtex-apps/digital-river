type Query {
  orderFormConfiguration: OrderFormConfiguration @cacheControl(scope: PRIVATE)
}

type Mutation {
  updateOrderFormConfiguration(
    orderFormConfiguration: OrderFormConfigurationInput!
  ): OrderFormConfiguration @cacheControl(scope: PRIVATE)
}
